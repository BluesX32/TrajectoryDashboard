% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trajectory.R
\name{compute_trajectory_phases}
\alias{compute_trajectory_phases}
\title{Compute trajectory phases from a lab time series}
\usage{
compute_trajectory_phases(
  lab_df,
  concept_id = NULL,
  window_days = 90L,
  min_observations = 2L,
  uln_override = NULL,
  slope_threshold_pct = 0.05,
  response_drop_pct = 0.3
)
}
\arguments{
\item{lab_df}{Tibble from \code{\link[=fetch_labs]{fetch_labs()}}. Required columns:
\code{measurement_date}, \code{value_as_number}, \code{measurement_concept_id}.
Optional: \code{range_high} (ULN). If absent or all-NA, \code{uln_override} is used.}

\item{concept_id}{Integer. Which lab concept to use (e.g. CK concept ID).
Use \code{NULL} to use the first concept found.}

\item{window_days}{Integer. Rolling window width in days. Default \code{90}.}

\item{min_observations}{Integer. Minimum points in a window to assign
a phase. Windows with fewer points receive \code{phase = "sparse"}.
Default \code{2}.}

\item{uln_override}{Numeric or \code{NULL}. If supplied, overrides \code{range_high}
from the data. Useful when the OMOP \code{range_high} column is missing or
unreliable.}

\item{slope_threshold_pct}{Numeric. A window's slope is considered
"rising" or "falling" only when the absolute rate exceeds this fraction
of ULN per day. Default \code{0.05} (5\% of ULN per day).}

\item{response_drop_pct}{Numeric. Minimum percentage drop from a flare/
worsening peak to classify a window as \code{"response"}. Default \code{0.30}.}
}
\value{
A tibble with one row per phase segment:
\code{window_start}, \code{window_end}, \code{phase}, \code{confidence}, \code{mean_value},
\code{slope_per_day}, \code{n_obs}, \code{is_sparse}, \code{uln}.
Consecutive windows with the same phase are consolidated into segments.
}
\description{
Applies a rolling-window algorithm to classify each time period into one
of six phases: \code{"flare"}, \code{"worsening"}, \code{"stable"}, \code{"response"},
\code{"relapse"}, or \code{"sparse"}. Sparse periods (< \code{min_observations} values
in the window) receive no phase assignment and are rendered as hatched
grey in the dashboard.
}
