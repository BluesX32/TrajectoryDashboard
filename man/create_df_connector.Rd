% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connector.R
\name{create_df_connector}
\alias{create_df_connector}
\title{Create an in-memory connector for tests and demos}
\usage{
create_df_connector(patient_data)
}
\arguments{
\item{patient_data}{A named list with any subset of these slots:
\verb{$labs}, \verb{$medications}, \verb{$conditions}, \verb{$visits}, \verb{$notes},
\verb{$observations}. Each slot must be a data frame (or tibble) conforming
to the column contract for that domain. Missing slots are treated as
empty (zero-row tibbles).}
}
\value{
An object of class \code{c("df_connector", "trajectory_connector")}.
}
\description{
Wraps a named list of pre-built domain tibbles in the standard connector
interface. No database or extra packages required.
}
\examples{
synth <- readRDS(system.file("extdata", "synthetic_patient_data.rds",
                             package = "TrajectoryDashboard"))
con <- create_df_connector(synth)
data <- fetch_patient_data(con, person_id = 1L)
}
