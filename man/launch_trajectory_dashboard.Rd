% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app.R
\name{launch_trajectory_dashboard}
\alias{launch_trajectory_dashboard}
\title{Launch the Patient Trajectory Dashboard}
\usage{
launch_trajectory_dashboard(
  connector = NULL,
  person_ids = NULL,
  port = NULL,
  launch_browser = TRUE,
  ...
)
}
\arguments{
\item{connector}{A \code{trajectory_connector} from \code{\link[=create_omop_connector]{create_omop_connector()}},
\code{\link[=create_df_connector]{create_df_connector()}}, or \code{\link[=create_omop_connection]{create_omop_connection()}}. If \code{NULL},
the bundled synthetic patient data is used (no database required).}

\item{person_ids}{Optional character vector of patient IDs to show in the
patient selector. If \code{NULL}, a free-text input is shown.}

\item{port}{Integer. Port for the Shiny server. Default \code{NULL} (auto).}

\item{launch_browser}{Logical. Open the app in a browser automatically.
Default \code{TRUE}.}

\item{...}{Additional arguments passed to \code{shiny::shinyApp()}.}
}
\value{
Launches a Shiny app; does not return a value.
}
\description{
Opens an interactive Shiny dashboard for longitudinal patient trajectory
visualization based on OMOP CDM data.
}
\details{
\subsection{Quick start}{

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Demo with synthetic data (no database required)
launch_trajectory_dashboard()

# Live OMOP database
con <- create_connection_from_env(".env")
launch_trajectory_dashboard(con)

# Restrict to specific patients
launch_trajectory_dashboard(con, person_ids = c("10001", "10002"))
}\if{html}{\out{</div>}}
}

\subsection{Input: environment file}{

Create a \code{.env} file in your working directory (see \code{.env.example}):

\if{html}{\out{<div class="sourceCode">}}\preformatted{SQL_SERVER=myserver.institution.edu
SQL_DATABASE=OMOP_CDM
SQL_CDM_SCHEMA=dbo
USE_WINDOWS_AUTH=true
}\if{html}{\out{</div>}}
}
}
\examples{
\dontrun{
# Demo mode with synthetic data
launch_trajectory_dashboard()

# Connect to real OMOP database
con <- create_connection_from_env(".env")
launch_trajectory_dashboard(con)
}
}
