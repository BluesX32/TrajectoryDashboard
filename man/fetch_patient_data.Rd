% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_patient.R
\name{fetch_patient_data}
\alias{fetch_patient_data}
\title{Fetch all OMOP domains for a single patient}
\usage{
fetch_patient_data(
  connector,
  person_id,
  start_date = "1900-01-01",
  end_date = NULL,
  domains = c("labs", "medications", "conditions", "visits", "notes", "observations"),
  lab_concepts = NULL,
  drug_concepts = NULL
)
}
\arguments{
\item{connector}{A \code{trajectory_connector} from \code{\link[=create_omop_connector]{create_omop_connector()}},
\code{\link[=create_df_connector]{create_df_connector()}}, or \code{\link[=create_omop_connection]{create_omop_connection()}}.}

\item{person_id}{Integer. Single patient identifier.}

\item{start_date}{Character or Date. Lower date bound. Default \code{"1900-01-01"}.}

\item{end_date}{Character or Date. Upper date bound. Default today.}

\item{domains}{Character vector. Which domains to fetch. Default: all six.
Subset to speed up data loading when not all domains are needed.}

\item{lab_concepts}{Integer vector or NULL. If supplied, restricts labs to
these \code{measurement_concept_id} values. Use \code{unlist(MYOSITIS_LAB_CONCEPTS)}
to fetch all myositis-relevant labs.}

\item{drug_concepts}{Integer vector or NULL. If supplied, restricts
medications to these \code{drug_concept_id} values.}
}
\value{
A named list with six tibble slots:
\verb{$labs}, \verb{$medications}, \verb{$conditions}, \verb{$visits}, \verb{$notes},
\verb{$observations}. Empty domains return a zero-row tibble with the
expected columns.
}
\description{
Retrieves labs, medications, conditions, visits, notes, and observations
for a single patient across a specified date range. For \code{omop_connector},
all 6 domain queries share one JDBC connection opened by this function.
}
\examples{
synth <- readRDS(system.file("extdata", "synthetic_patient_data.rds",
                             package = "TrajectoryDashboard"))
con  <- create_df_connector(synth)
data <- fetch_patient_data(con, person_id = 1L)
names(data)  # "labs" "medications" "conditions" "visits" "notes" "observations"
}
