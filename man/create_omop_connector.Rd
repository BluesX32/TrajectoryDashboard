% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connector.R
\name{create_omop_connector}
\alias{create_omop_connector}
\title{Create an OMOP CDM connector for live database access}
\usage{
create_omop_connector(
  connectionDetails,
  cdm_schema,
  vocab_schema = NULL,
  results_schema = NULL,
  temp_schema = NULL,
  cdm_version = "5.4"
)
}
\arguments{
\item{connectionDetails}{A \code{connectionDetails} object from
\code{DatabaseConnector::createConnectionDetails()}.}

\item{cdm_schema}{\code{character(1)}. Schema containing OMOP CDM tables.}

\item{vocab_schema}{\code{character(1)} or \code{NULL}. Vocabulary schema.
Defaults to \code{cdm_schema}.}

\item{results_schema}{\code{character(1)} or \code{NULL}. Schema for cohort/results.}

\item{temp_schema}{\code{character(1)} or \code{NULL}. For temp-table emulation.}

\item{cdm_version}{\code{character(1)}. OMOP CDM version. Default \code{"5.4"}.}
}
\value{
An object of class \code{c("omop_connector", "trajectory_connector")}.
}
\description{
Constructs a connector object holding connection details and schema
information for a live OMOP CDM database. The database connection is opened
lazily -- only when a query is executed -- and closed immediately after via
\code{\link[=with_connector]{with_connector()}}. Alternatively, use \code{\link[=create_omop_connection]{create_omop_connection()}} which
opens a persistent connection.
}
